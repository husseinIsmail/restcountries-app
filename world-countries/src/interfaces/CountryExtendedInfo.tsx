export interface CountryExtendedInfo {
  name: string,
  nativeName: string,
  region: string,
  subregion: string,
  capital: string,
  topLevelDomain: string,
  currencies: string[],
  languages: string[],
  borders: string[]
};
